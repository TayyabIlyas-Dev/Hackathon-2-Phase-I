[project]
name = "todo-cli-app"
version = "0.1.0"
description = "A command-line Todo application"
readme = "README.md"
requires-python = ">=3.13"
license = {text = "MIT"}
authors = [
    {name = "Developer", email = "dev@example.com"}
]